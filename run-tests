#!/bin/bash

set -e

for d in *
do
    if [[ -f $d/Makefile ]]
    then
        (cd $d >/dev/null ; make -s test)
    elif [[ -f $d/mvnw ]]
    then
        (cd $d >/dev/null ; ./mvnw -q clean verify)
    fi
done
